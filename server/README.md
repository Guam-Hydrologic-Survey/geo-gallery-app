# Server 

_The backend code._

## Install Dependencies 

First, ensure you are in the `/server` directory. 

```
npm install 
```

> View all `server` dependencies in `package.json` file. 

## Start development server 

```
npm run dev
```

> Alternatively, you can use `nodemon server.js`.