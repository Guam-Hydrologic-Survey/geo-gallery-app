# Client 

_The frontend code._

## Install dependencies 

First, ensure that you are in th `/client` directory. 

```
npm install 
```

> View all `client` dependencies in `package.json` file. 

## Start development server 

```
npm run dev
```

> Alternatively, you can run `npx vite` or simply `vite`. 